function calculateArea2(radius: number): number {
  //  // Лучше использовать встроенную константу Math.PI для повышения точности и читаемости или создать константу самостоятельно
  return 3.14 * radius * radius; // "волшебное число" 3.14
}
